\documentclass[11pt,a4paper]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{xcolor}
\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue!60!black]{hyperref}
\usepackage{microtype}

\graphicspath{{../results/}}

\newcommand{\shrink}{\mathrm{shrink}}
\newcommand{\trcov}{\operatorname{tr}\mathrm{Cov}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\operatorname{Var}}
\newcommand{\todo}[1]{\textcolor{red}{[\,#1\,]}}

\title{\bfseries Shrinkage Baselines for GRPO on GSM8K\\
\large Does James--Stein shrinkage of the group mean improve GRPO?}
\author{Hanwen Yang}
\date{\today}

\begin{document}
\maketitle

\begin{abstract}
\noindent
GRPO estimates each prompt's baseline from the $N$ completions sampled for
that prompt alone; with $N$ small this mean is noisy. The assignment proposes
replacing it with a James--Stein estimator that shrinks each group mean toward
the batch mean. I implemented the specification, six baselines behind one
registry, and measured them on Qwen2.5-0.5B-Instruct / GSM8K on a single
RTX~5080.

Three findings. \textbf{(i)} The specification as written does not work: its
$V=1/N$ assumes unit per-sample reward variance, but $0/1$ correctness rewards
have $p(1-p)\le\tfrac14$. The shrinkage factor collapses and the estimator
degenerates into the global mean --- and it does so \emph{monotonically in
$K$}, so it fails harder at the batch sizes GRPO is actually deployed at.
\textbf{(ii)} Estimating $V$ from the data instead reproduces the published
claim: the two-level leave-one-out variant reaches
$-21.4\%$ $[-51.5,-7.8]$ gradient noise, inside the $11.2$--$67.1\%$ band
reported by Zeng et al.~\cite{zeng2025shrinking}. \textbf{(iii)} The
specification's optional ``divide by the within-group standard deviation'' is
unsafe in combination with shrinkage, inflating advantages by $10^4$.

I also report a measurement I think matters more than any of the above: two
runs of the \emph{same} configuration at the \emph{same} seed end $3.0$
accuracy points apart. The effect under test is $0.6$--$1.0$ points. This is
why the headline measurement here is gradient noise on paired rollouts rather
than end-to-end accuracy.
\end{abstract}

\section{Task}

Given \texttt{GRPO.md}: replace GRPO's per-group mean baseline with a
James--Stein shrinkage estimator, on GSM8K, with a small policy model. The
specification places the change precisely --- ``after the reward matrix is
formed and before advantages are computed'' --- so what is being rewritten is
the \emph{baseline}, not the reward function.
\todo{keep this distinction; it was ambiguous in the original brief}

\paragraph{Setup.} Qwen2.5-0.5B-Instruct (full fine-tune, no LoRA), GSM8K
(7473 train / 1319 test), rule-based verifiable reward (no reward model), one
RTX~5080 (16\,GB, sm\_120). $K=8$ prompts $\times\,N=8$ generations, 512 new
tokens, bf16 with gradient checkpointing; 11.0\,GiB peak, ${\sim}25$\,s/step.

\section{Method}

\subsection{What the specification says}

With group means $X_k=\frac1N\sum_i r_i^{(k)}$ and grand mean $\bar X$,
\begin{equation}
\tilde\mu_k=\bar X+\shrink\cdot(X_k-\bar X),
\qquad
\shrink=\max\!\left(0,\;1-\frac{c}{S}\right),
\label{eq:js}
\end{equation}
where $S=\sum_k (X_k-\bar X)^2$ and $c=V(K-3)$ with $V=\Var(X_k)$ assumed
equal to $1/N$. Advantages are $A_i^{(k)}=r_i^{(k)}-\tilde\mu_k$.

\subsection{The assumption that fails}

$V=1/N$ presumes per-sample reward variance $\sigma^2\approx1$. On GSM8K the
reward is binary, so $\sigma_k^2=p_k(1-p_k)\le\tfrac14$ --- at least $4\times$
smaller, and heteroscedastic across prompts (zero for a group that is entirely
right or entirely wrong). Overstating $V$ overstates $c$, which drives
$\shrink$ to zero.

The failure is not incidental; it is asymptotic in $K$. Since
$\E[S]\approx(K-1)\Var(X_k)$ and $c=V(K-3)$,
\begin{equation}
\frac{c}{S}\;\xrightarrow[K\to\infty]{}\;
\frac{V_{\text{assumed}}}{\Var(X_k)}
=\frac{1/N}{\tau^2+\sigma^2/N},
\label{eq:limit}
\end{equation}
a constant. Whenever it exceeds 1, $\shrink\to0$ and the estimator becomes the
global mean. For the reward distribution used here
($V_{\text{assumed}}=0.125$, $\Var(X_k)=0.074$) the limit is $1.69>1$: the
collapse is certain, and small $K$ only escapes it by sampling noise in $S$.

\subsection{Baselines compared}

\begin{table}[h]\centering\small
\begin{tabular}{llp{6.6cm}}
\toprule
name & baseline $b[k,i]$ & role \\
\midrule
\texttt{vanilla}   & group mean                & GRPO, the incumbent \\
\texttt{rloo}      & leave-one-out group mean  & unbiased variance-reduction control \\
\texttt{global}    & batch mean                & \textbf{ablation}: what over-shrinkage degenerates to \\
\texttt{js\_fixed} & Eq.~\eqref{eq:js}, $V=1/N$ & the specification as written \\
\texttt{js\_pooled}& Eq.~\eqref{eq:js}, $V$ estimated & proposed correction \\
\texttt{js\_loo}   & two-level leave-one-out shrinkage & Zeng et al.~\cite{zeng2025shrinking} \\
\bottomrule
\end{tabular}
\caption{The \texttt{global} arm is the one that makes the comparison
interpretable: without it, ``James--Stein helps'' cannot be told apart from
``any baseline other than the group mean helps''.}
\end{table}

Implementation follows verl's \texttt{register\_adv\_est} registry so any
estimator here drops into that framework through a thin adapter. 115 tests,
including a literal transcription of the specification's ten steps asserted
against the implementation to $10^{-12}$.

\section{Result 1: the specification collapses onto the global mean}

Ground truth is unavailable on GSM8K, so this is measured in simulation:
$p_k\sim\mathrm{Beta}(1.2,2.2)$ (mean $0.35$, matching the model's observed
per-prompt spread), rewards $\sim\mathrm{Bernoulli}(p_k)$.

\begin{table}[h]\centering\small
\begin{tabular}{rrrrrr}
\toprule
$K$ & \texttt{js\_fixed} $\shrink$ & \texttt{js\_fixed} MSE & \texttt{global} $\shrink$ & \texttt{global} MSE & \texttt{js\_pooled} MSE \\
\midrule
4  & 0.377 & $+0.4\%$   & 0.049 & $+100.6\%$ & $-9.5\%$ \\
8  & 0.079 & $+86.4\%$  & 0.023 & $+118.7\%$ & $-18.7\%$ \\
16 & 0.016 & $+121.1\%$ & 0.010 & $+125.0\%$ & $-23.8\%$ \\
32 & 0.006 & $+130.8\%$ & 0.006 & $+130.9\%$ & $-26.7\%$ \\
64 & 0.003 & $+132.1\%$ & 0.003 & $+132.1\%$ & $-28.4\%$ \\
\bottomrule
\end{tabular}
\caption{MSE of the baseline against the true $p_k$, relative to
\texttt{vanilla}; $N=8$, 3000 trials. By $K=32$ \texttt{js\_fixed} and
\texttt{global} agree to three significant figures --- they are the same
estimator. \texttt{js\_pooled} moves the other way, improving as $K$ grows.}
\end{table}

Two consequences worth stating plainly. First, \textbf{the specified method
gets worse as the batch holds more prompts}, which is the opposite of what
more data should buy, and it is the regime GRPO is deployed in. Second, at
$K=4$ it is roughly neutral --- so a small-$K$ experiment would show nothing
wrong.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{fig1_synthetic_mse.pdf}
\caption{Sweeping $N$ at $K=32$. \texttt{js\_fixed} (dashed) lies exactly on
\texttt{global} for $N\le8$. Estimating $V$ recovers the intended behaviour,
and its advantage grows as $N$ shrinks --- the regime the method targets.}
\end{figure}

\section{Result 2: estimating $V$ reproduces the published claim}

Comparing training runs is the wrong instrument for this (Sec.~\ref{sec:floor}).
Instead every baseline scores the \emph{same} rollouts, so rollout randomness
cancels and the baseline is the only thing that differs; the policy is frozen,
so this measures the estimator rather than a training outcome. The reported
quantity is $\trcov(g)/\lVert\E[g]\rVert^2$ --- scale-invariant, because the
baselines produce gradients whose magnitudes differ ${\sim}5\times$ and raw
$\trcov$ would merely rank them by scale.

\begin{table}[h]\centering\small
\begin{tabular}{lrrrl}
\toprule
baseline & noise/signal & vs GRPO & 95\% CI & \\
\midrule
\texttt{vanilla}    & 11.5 & ---        & ---              & \\
\texttt{rloo}       & 11.5 & $-0.3\%$   & $[-2.5,+6.9]$    & no difference (instrument check) \\
\texttt{js\_loo}    & \textbf{9.1} & $\mathbf{-21.4\%}$ & $\mathbf{[-51.5,-7.8]}$ & significant \\
\texttt{js\_pooled} & 9.9  & $-14.4\%$  & $[-38.2,+0.8]$   & marginal \\
\texttt{js\_fixed}  & 6.0  & $-48.1\%$  & $[-81.3,-15.8]$  & biased --- see below \\
\texttt{global}     & 5.8  & $-49.7\%$  & $[-83.5,-15.6]$  & biased --- see below \\
\bottomrule
\end{tabular}
\caption{200 paired batches, $K=8\times N=8$, mean accuracy 29.1\%. Intervals
are a paired bootstrap over batches.}
\end{table}

\paragraph{\texttt{js\_fixed} and \texttt{global} look best here, and that
reading is wrong.} Only leave-one-out baselines are independent of the reward
they are subtracted from, so only \texttt{rloo} and \texttt{js\_loo} are
unbiased for the same gradient. The other two target a \emph{different}
$\E[g]$: handing an all-correct group a positive advantage inflates
$\lVert\E[g]\rVert^2$ with a component that merely pushes up easy prompts. A
larger denominator is not more signal.

That \texttt{rloo}$\,\approx\,$\texttt{vanilla} to within $[-2.5,+6.9]$ is the
check that the instrument resolves anything at all --- and it agrees with the
synthetic study, where leave-one-out changes the baseline's independence, not
its accuracy.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{fig2_grad_variance.pdf}
\caption{Effect sizes with bootstrap intervals. Shaded: the band reported by
Zeng et al. Hollow markers are biased estimators, not comparable at face value.}
\end{figure}

\section{Result 3: the standard-deviation variant is a footgun}

The specification offers ``divide by the within-group standard deviation
afterwards'' as an option. Under plain GRPO that is safe on a degenerate group
only because the advantage is exactly zero there, so $0/(0+10^{-4})=0$. Every
shrinkage baseline gives such a group a \emph{non-zero} advantage by design ---
that is the point of the method --- so the same expression multiplies it by
$10^4$. Measured on GSM8K at $K=8,N=8$ with 38\% degenerate groups:
$\Var(A)=1.07\times10^5$, $\lVert g\rVert=1.7\times10^3$. Leaving zero-spread
groups unscaled gives $0.57$ and $1.2$.

\section{Why not end-to-end accuracy}
\label{sec:floor}

\begin{table}[h]\centering\small
\begin{tabular}{lrr}
\toprule
& run A & run B \\
\midrule
eval@0     & 42.5\% & 42.5\% \\
eval@50    & 44.5\% & 43.5\% \\
eval@100   & 44.5\% & 44.5\% \\
eval@final & \textbf{48.5\%} & \textbf{45.5\%} \\
\bottomrule
\end{tabular}
\caption{Same arm, same seed, same configuration. \texttt{eval@0} matches
exactly because greedy decoding is deterministic, so the divergence is entirely
in training: \texttt{torch.manual\_seed} does not make sampled generation
reproducible on GPU.}
\end{table}

The noise floor is $3.0$ points. The effect reported for this exact setting is
$0.6$--$1.0$ points \cite{zeng2025shrinking} --- three to five times smaller.
End-to-end accuracy cannot resolve it on one GPU, and any single-seed accuracy
comparison at this scale, in either direction, is an artefact.

\section{Training runs}

Five arms, 150 steps each, identical seed so every arm walks the same prompt
stream (verified: identical reward and degenerate-group fractions at step~0).
Every arm evaluates the same 200 test problems in the same order, so
checkpoints are compared with McNemar rather than by overlapping binomial
intervals.

\begin{table}[h]\centering\small
\begin{tabular}{lrrrrrr}
\toprule
arm & eval@0 & eval@final & $\Delta$ & $p$ & $\shrink$ & collapsed \\
\midrule
\texttt{vanilla}    & 42.5\% & 45.5\% & $+3.0$ & 0.24 & 1.000 & 0/150 \\
\texttt{js\_pooled} & 42.5\% & 45.0\% & $+2.5$ & 0.27 & 0.850 & 1/150 \\
\texttt{js\_loo}    & 42.5\% & 45.5\% & $+3.0$ & 0.21 & 0.818 & 0/150 \\
\texttt{js\_fixed}  & 42.5\% & 47.0\% & $+4.5$ & 0.11 & 0.202 & 64/150 \\
\texttt{global}     & 42.5\% & 47.5\% & $+5.0$ & 0.06 & 0.025 & 133/150 \\
\bottomrule
\end{tabular}
\caption{$p$ is a paired McNemar of each arm's final checkpoint against its own
\texttt{eval@0}. $\shrink$ is the mean over the last 30 steps; ``collapsed''
counts steps with $\shrink<0.01$.}
\label{tab:sweep}
\end{table}

\paragraph{Nothing here is significant, in either direction.} No arm improves
on its own starting point at $p<0.05$, and no arm differs from
\texttt{vanilla}: against \texttt{vanilla}'s final checkpoint,
\texttt{global} is $+2.0$ ($p=0.54$), \texttt{js\_fixed} $+1.5$ ($p=0.68$),
\texttt{js\_loo} $+0.0$ ($p=1.00$), \texttt{js\_pooled} $-0.5$ ($p=1.00$).
This is exactly what Sec.~\ref{sec:floor} predicts: the spread across all five
arms is 2.5 points, below the 3.0-point floor a single arm shows against
itself. \textbf{The sweep does not show that shrinkage helps training, and it
was not capable of showing it.}

It is worth naming the trap in Table~\ref{tab:sweep}: \texttt{global} has the
largest nominal gain. Read without the $p$ column it says ``throw away the
group structure entirely and GRPO improves'', which is both unsupported and
contrary to Sec.~\ref{sec:floor}.

\paragraph{What the sweep does establish is structural.} The shrinkage column
separates cleanly into three tiers that accuracy cannot resolve but the
estimator's own behaviour makes unambiguous:

\begin{center}\small
\texttt{vanilla} $1.000$ \quad$\gg$\quad
\texttt{js\_pooled} $0.850$, \texttt{js\_loo} $0.818$ \quad$\gg$\quad
\texttt{js\_fixed} $0.202$ \quad$\gg$\quad \texttt{global} $0.025$
\end{center}

The two corrected variants retain ${\sim}82$--$85\%$ of the group structure ---
mild shrinkage, which is what Stein's result asks for --- while the specified
estimator retains $20\%$ and discards it outright on $43\%$ of steps. That
\texttt{js\_pooled} and \texttt{js\_loo}, derived independently, land within
$0.03$ of each other is mutual corroboration; it also raises confidence in the
$(n-1)/n$ factor I could not verify against the paper.

At $K=8$, \texttt{js\_fixed} is degraded but not identical to \texttt{global}
($0.202$ vs $0.025$). Full coincidence needs larger $K$, as Table~2 shows ---
which is the deployment regime, not this one.

\begin{figure}[h]\centering
\includegraphics[width=\textwidth]{fig3_training.pdf}
\caption{All five arms. Reward and degenerate-group panels overlap almost
exactly; the shrinkage panel separates into three tiers. Faint lines are
per-step values, bold is a rolling window --- median for the gradient norm,
which is spiky enough that a rolling mean manufactures flat plateaus that read
as stalled training.}
\end{figure}

\section{Limitations}

\begin{itemize}\itemsep2pt
\item \textbf{No end-to-end accuracy claim.} The sweep confirms this
      empirically: all five arms sit within 2.5 points, every $p\ge0.21$.
      Settling it needs multiple seeds per arm, which one GPU cannot supply.
\item \textbf{48 batches was not enough} for the gradient measurement --- every
      interval crossed zero. The reported result needed 200. Effect sizes should
      not be read off short runs here.
\item \textbf{\texttt{js\_loo}'s $(n-1)/n$ factor is unverified} against the
      paper's PDF (OpenReview blocks automated access); it was transcribed from
      the arXiv HTML. \texttt{baselines.py} exposes a switch. That
      \texttt{js\_pooled}, derived independently, lands nearby (9.9 vs 9.1) is
      reassuring but not a substitute. \todo{check against the PDF}
\item \textbf{Single model, single task.} Whether the collapse threshold moves
      with reward density is untested.
\end{itemize}

\section{Reproduction}

\begin{verbatim}
uv sync && python main.py smoke        # torch cu129; the RTX 5080 is sm_120
python -m pytest tests/ -q             # 115 tests
python main.py synthetic --k 4 8 16 32 64 --n 8
python main.py gradvar --batches 200
python main.py sweep --baselines vanilla js_fixed js_pooled js_loo global
python main.py compare
\end{verbatim}

\todo{repo link}
\todo{single seed per arm; a second seed would at least bound the run-to-run term}

\begin{thebibliography}{9}\small
\bibitem{shao2024deepseekmath} Z. Shao et al.
  \emph{DeepSeekMath: Pushing the Limits of Mathematical Reasoning in Open
  Language Models}. arXiv:2402.03300, 2024.
\bibitem{zeng2025shrinking} G. Zeng, Z. Zhou, D. Arora, A. Zanette.
  \emph{Shrinking the Variance: Shrinkage Baselines for Reinforcement Learning
  with Verifiable Rewards}. arXiv:2511.03710, 2025.
\bibitem{han2026ebpo} K. Han et al.
  \emph{EBPO: Empirical Bayes Shrinkage for Stabilizing Group-Relative Policy
  Optimization}. arXiv:2602.05165, 2026.
\bibitem{james1961} W. James, C. Stein.
  \emph{Estimation with Quadratic Loss}. Proc.\ 4th Berkeley Symp., 1961.
\bibitem{efron1977} B. Efron, C. Morris.
  \emph{Stein's Paradox in Statistics}. Scientific American, 1977.
\bibitem{liu2025drgrpo} Z. Liu et al.
  \emph{Understanding R1-Zero-Like Training: A Critical Perspective}.
  arXiv:2503.20783, 2025.
\end{thebibliography}

\end{document}
